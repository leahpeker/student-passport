# Claude via AWS Bedrock. Credentials come from your AWS profile, not from here.
# Export AWS_PROFILE=dev-engineer (or run `aws sso login`) before starting the server.
BEDROCK_REGION=us-west-2
BEDROCK_MODEL=anthropic.claude-opus-5

DJANGO_SECRET_KEY=change-me
DJANGO_DEBUG=1
